# Suivi des écarts — évolutions non prévues au cahier des charges

> Copie de référence de `E:\xampp\htdocs\kaeged\docs\evolutions-cahier-des-charges.md` (source de vérité).
> Dernière synchro : 2026-09-04.

Ce document trace **toutes les évolutions implémentées qui n'étaient pas prévues**
dans `spec/cahier-des-charges-ged-saas-v1.0.docx` (12/08/2026).
Il doit être **mis à jour à chaque itération** (règle de travail permanente).

## Écarts implémentés

| # | Date | Évolution | Écart vs CDG | Impact |
|---|------|-----------|--------------|--------|
| 1 | 08/2026 | **Viewers/éditeurs en ligne** (CodeMirror, pdf.js+pdf-lib, docx-preview, SheetJS, pptxjs) | CDG §18 prévoyait OnlyOffice/fallback ; pas de viewers embarqués | Aperçu et édition texte/PDF sans serveur externe |
| 2 | 08/2026 | **Éditeur PDF annotable** (texte, surlignage, notes) | Non prévu (CDG §18 = édition Office uniquement) | Annotation PDF en ligne |
| 3 | 08/2026 | **Partage externe par jeton** (RM-012, expiration, mot de passe) | CDG §22 le laissait « si introduit ultérieurement », désactivé par défaut | Liens externes sécurisés implémentés |
| 4 | 08/2026 | **Options paramétrables étendues** (MIME autorisés, verrouillage auto, commentaire obligatoire, branding, langue/fuseau) | CDG §55 listait des décisions mais pas d'écran paramètres riche | Administration paramétrable complète |
| 5 | 08/2026 | **Paramètres plateforme** super admin (défauts nouveaux tenants) | Non prévu | Pré-remplissage des tenants |
| 6 | 08/2026 | **Padding anti-IDM** sur le streaming PDF | Non prévu (contournement technique) | Contre les gestionnaires de téléchargement |
| 7 | 08/2026 | **Rôles de groupe** (`group_role`) — héritage des rôles par appartenance | CDG §12/13 ne prévoyait que des rôles directs | RBAC enrichi |
| 8 | 08/2026 | **Sous-menu Administration** dans la sidebar | Non prévu (UX) | Navigation |
| 9 | 08/2026 | **Périmètre V02** : référentiels (domaines, processus, postes, sites…), champs dimensions, workflow 7 statuts, menu « Mes documents », accusés de lecture, import CSV registre, alertes de revue | Nouveau périmètre client (GED opérationnelle V02) | Adaptation majeure |
| 10 | 08/2026 | **Édition des caractéristiques du document** (Titre, Espace, Dossier, Type documentaire) depuis l'onglet Métadonnées — auparavant fixées à l'import | CDG ne prévoyait que la modification des métadonnées personnalisées et de la confidentialité | Gestion documentaire complète (garde serveur dossier↔espace) |
| 11 | 08/2026 | **Cycle de vie documentaire — gel par statut** : documents approuvés, archivés, expirés ou obsolètes verrouillés (métadonnées + contenu) ; une nouvelle révision est requise ; UI avec bandeaux explicatifs | CDG ne verrouillait pas les documents par statut (règle 6/7 V02 non implémentée) | Conformité GED classique (intégrité des documents validés) |
| 12 | 08/2026 | **Renommage Open-CoolGed + nom personnalisable** : défaut `Open-CoolGed`, surcharge par le superadmin (nom plateforme) et par chaque admin tenant (`branding.brand_name`, vide = héritage) | KAE GED était le nom historique ; le CDG ne prévoyait pas de nom par tenant | Identité par entreprise (login, titres, sidebar, emails) |
| 13 | 08/2026 | **Design Kami** : restyle complet de l'interface (palette #f5f4ed/#efece2/#1b365d, typographie Georgia, angles vifs) basé sur spec/documentation-kami.html | CDG ne spécifiait pas de charte graphique | Identité visuelle cohérente |
| 14 | 08/2026 | **Messagerie SMTP par tenant** : onglet « Messagerie » (host/port/user/password chiffré/from), test d'envoi, notifications envoyées par email si activé | CDG prévoyait les notifications en base uniquement ; l'email dépendait du SMTP serveur | Notifications réellement délivrées par email |
| 15 | 08/2026 | **Thèmes visuels + mode sombre** : 12 thèmes (palettes claire/sombre, polices Google Fonts, radius) ; mode clair/sombre/auto (préférence utilisateur > tenant > plateforme) ; thème personnel par utilisateur (profil, avec aperçu pastilles + police) ; sélecteurs avec aperçu (Branding tenant + plateforme) ; profil enrichi (apparence, changement de mot de passe, MFA, infos) ; toggle sidebar | CDG ne spécifiait pas de charte graphique ni de mode sombre ; pas de changement de mot de passe en self-service | Identité visuelle personnalisable + confort (mode sombre) + autonomie utilisateur |
| 16 | 08/2026 | **Fournisseurs LLM réels (OpenAI/Anthropic)** : adaptateurs HTTP (chat completions / messages API), clés API chiffrées configurables (plateforme héritées + surcharge tenant), modèles paramétrables, boutons « Tester la connexion » (superadmin + tenant), sélection du premier fournisseur autorisé avec clé (repli mock) | CDG §35 ne prévoyait que le fournisseur mock | IA réellement opérationnelle (résumé, QA, classification, extraction, correction, comparaison, traduction, OCR) |
| 17 | 08/2026 | **Catalogue étendu de fournisseurs LLM** : registry `config/llm.php` (12 fournisseurs — OpenAI, Anthropic, Gemini, Groq, DeepSeek, Mistral, OpenRouter, Ollama, xAI, Azure, Fireworks, Together), driver générique `openai_compat` (1 adaptateur pour tous les compatibles), adaptateur natif Gemini, multi-select dans l'UI (superadmin + tenant), champs clé/modèle dynamiques par fournisseur | CDG §35 ne prévoyait que le fournisseur mock | Choix du fournisseur LLM par l'organisation |
| 18 | 08/2026 | **Liste Documents enrichie** : colonnes de métadonnées optionnelles (cases à cocher dans le sélecteur « Colonnes métadonnées », persistance session, pagination) + tri des colonnes du tableau (liste blanche sécurisée, asc/desc avec indicateur ▲▼, tri par Titre/Espace/Type/Statut/Mis à jour, **tri par métadonnées triables avec CAST typé — numérique/date/texte — et NULLS LAST portable**, URL préservée) | CDG ne prévoyait pas de personnalisation d'affichage ni de tri dans la liste | Consultation des documents adaptée à l'utilisateur |
| 19 | 08/2026 | **Pagination Bootstrap 5** : `Paginator::useBootstrapFive()` (le view Tailwind par défaut s'affichait sans CSS Tailwind — flèches SVG décalées vers le bas) + surcharges CSS pagination alignées sur le thème (clair/sombre) | Non prévu (défaut Laravel inadapté au thème) | Navigation de liste lisible et cohérente |
| 20 | 08/2026 | **Corrections tri liste Documents** : `select('documents.*')` sur les jointures espace/type (l'hydratation Eloquent mélangeait les id — valeurs de métadonnées répétées/désordonnées) ; tri métadonnées `meta:{id}` validé contre les définitions (triable sans sélection préalable, colonne ajoutée implicitement) et alternance ▲▼ corrigée | Bugs découverts après la mise en production de l'écart n°18 | Tri fiable et valeurs métadonnées correctes |
| 21 | 08/2026 | **Import CSV (interface web)** : menu Administration → « Import CSV » — formulaire d'upload (CSV point-virgule), template téléchargeable (en-tête 14 colonnes + exemples), création auto espaces/dossiers/types, colonne `fichier` optionnelle (chemin serveur, tous formats MIME autorisés attachés via addVersion, version 1.0), rapport ok/erreurs/avertissements ; commande CLI `v02:import` étendue à la colonne `fichier` | CDG §18.3 ne prévoyait qu'une commande CLI sans fichiers | Import du registre maître avec fichiers, accessible aux administrateurs sans terminal |
| 22 | 08/2026 | **Vue cartes (Documents & Mes documents)** : bascule liste ⇄ cartes (persistance session), sélecteur « Regrouper par » (espace/section, type/famille, statut, confidentialité, criticité, domaines, processus, référentiels poste→pays, métadonnées personnalisées), cartes de regroupement avec comptage (respectant les droits et les filtres actifs, « Non renseigné » inclus), clic sur une carte → liste filtrée avec badge du regroupement + retour aux cartes | CDG ne prévoyait que la liste en tableau | Consultation des documents par regroupements métadonnées |
| 23 | 08/2026 | **Correctif navigation viewer embarqué** : les liens « Retour » et « Fiche document » des pages standalone (layouts/standalone + office/viewer) ciblent désormais `target="_top"` — la fiche document embarque le viewer dans un iframe, et sans cela la fiche complète (menu/sidebar) se rechargeait **dans** le frame, dupliquant les menus | Bug constaté : imbrication de la fiche dans son propre aperçu (iframe) | Navigation cohérente depuis l'aperçu |
| 24 | 08/2026 | **Liaison des dimensions aux documents** : création manuelle (formulaire « Importer » : select Domaine, Processus, référentiels d'application poste→pays liés au pivot `document_referential`), import CSV étendu (`processus` + `application` type:nom;type:nom — colonnes optionnelles, `domaine` lie désormais réellement le document, template 16 colonnes, rétrocompatible 14 colonnes), fiche → onglet Métadonnées (modification des liens, gel/permissions inchangés), écran Utilisateurs admin (dimensions poste→pays assignables aux utilisateurs, alimente « Mes documents ») — les cartes Domaine/Processus/Poste→Pays comptent désormais les documents réellement liés | CDG ne liait ces dimensions que par l'import CSV (et sans lien réel pour domaine) ; aucune UI de saisie | Regroupements cartes exploitables + filtrage « Mes documents » par dimensions réelles |
| 25 | 08/2026 | **Commande `roles:sync` + navigation super admin** : `roles:sync` synchronise les rôles système sur tous les tenants (crée les rôles absents, ré-ajoute les permissions manquantes — ex. `admin.referentials` ajoutée après la création des tenants — `--dry-run`, idempotente) ; la sidebar s'affiche désormais aussi au super admin avec le sous-menu Administration complet (il pouvait tout voir via `PermissionService::can` mais n'avait aucune navigation UI) ; les opérations Référentiels s'exécutent dans le contexte du tenant de rattachement du super admin (`asTenantAdmin`) car `SetTenantContext` met un contexte null pour lui (RM-003 préservé : pas d'accès au contenu par défaut) — sans cela, les requêtes scopées renvoyaient 404 | Permission ajoutée après la création des tenants → rôles en dérive ; super admin sans UI d'administration | Rôles système toujours à jour ; super admin administre via l'interface |
| 26 | 08/2026 | **Titres des cartes de regroupement non tronqués** : suppression de la classe `text-truncate` (nowrap + ellipsis) sur le `<h6>` du partial `documents/_cards` — libellés affichés en entier, multi-lignes (`white-space: normal`, `overflow-wrap: anywhere` pour les mots longs), grille responsive et badge de comptage inchangés ; `title` conservé pour info-bulle | `text-truncate` coupait les libellés longs avec « … » | Lisibilité des regroupements (Documents & Mes documents) |
| 27 | 08/2026 | **Localisation FR/EN + préférences vues persistées + cloche notifications** : (a) `resources/lang/fr.json` + `en.json` (Laravel JSON), middleware `app.locale` (user.locale > tenant.settings.language > APP_LOCALE), sélecteur de langue navbar (route `profile.locale`, persistant), colonne `users.locale`, écrans principaux traduits (layout, login, dashboard, documents liste/cartes/fiche/formulaire, V02, notifications, référentiels, import CSV) — les textes non traduits restent en français ; (b) type de vue et regroupement persistés **en base** (`users.doc_view`/`doc_group`, colonnes) au lieu de la session — survit à la déconnexion et au changement d'appareil ; (c) **cloche de notifications** avec badge de comptage des non lues dans la sidebar + **polling toutes les 60 s** (route `GET /notifications/unread-count` → JSON, mise à jour sans rechargement) | CDG ne prévoyait ni localisation, ni préférences d'affichage durables, ni compteur de notifications | Interface bilingue, préférences durables, alertes visibles |
| 28 | 08/2026 | **Colonnes configurables enrichies (Documents + Mes documents)** : Documents — sélecteur de colonnes étendu aux référentiels V02 (Domaine, Processus, Poste, Département, Direction, Site, Entité, Pays) en plus des métadonnées personnalisées ; Mes documents — nouveau sélecteur « Colonnes à afficher… » (champs V02, référentiels et métadonnées personnalisées), valeurs absentes `—`, persistance distincte par utilisateur en base (`users.doc_columns` / `users.my_doc_columns`), tri sécurisé asc/desc pour domaine/processus/propriétaire/dates/criticité/métadonnées ; filtres, pagination, vues liste/cartes préservés | CDG ne prévoyait pas la personnalisation de colonnes V02 ni une sélection distincte par page | Consultation des registres adaptable à chaque utilisateur |
| 29 | 08/2026 | **Espaces personnels privés** : création automatique d’un espace `Personnel — {Nom}` par utilisateur (marqueurs techniques `is_personal`/`personal_user_id`), création de document Personnel ou Partagé (personnel présélectionné en UI et imposé côté serveur), menu « Mes documents personnels », isolation propriétaire avec supervision admin configurable (`personal_spaces_admin_access`, actif par défaut), publication contrôlée dans un espace partagé avec audit | CDG ne prévoyait pas de stockage personnel isolé | Documents individuels séparés de la GED collaborative |
| 30 | 09/2026 | **Correctif page Administration → Utilisateurs (HTML/UX)** : les modales « Modifier »/« MDP » étaient placées **dans** `<tbody>` (HTML invalide) — le parseur les sortait du tableau (« foster parenting »), rendu instable et modales qui semblaient faire disparaître la page. Modales déplacées hors du tableau (IDs uniques conservés, `tabindex="-1"`), boutons déclencheurs et d'actions passés en `type="button"` (aucun submit involontaire), boutons d'envoi en `type="submit"` explicite | Bug constaté : page instable / disparition au clic sur Modifier | Page Utilisateurs stable, édition fiable |
| 31 | 09/2026 | **Reset des contenus d'un tenant** : service `TenantResetService` (purge par tenant_id des documents, versions, fichiers storage, espaces/dossiers partagés, types, métadonnées, tags, référentiels, workflows, notifications, partages, audits, jobs IA — conservation du tenant, utilisateurs, rôles, paramètres, branding), **sauvegarde préalable obligatoire** (`BackupService::run`, reset annulé si échec), recréation automatique des espaces personnels ; **UI super admin** (écran Tenants → « Réinitialiser », confirmation par saisie du slug, audit `superadmin.tenant.reset`), **UI admin tenant** (Paramètres → « Zone dangereuse — vider le contenu », confirmation slug, audit `tenant.content_reset`) ; **CLI** `tenant:reset {tenant} [--dry-run] [--force]` | CDG ne prévoyait pas de remise à zéro du contenu d'un tenant | Recette / remise en service rapide d'un tenant sans recréation |
| 32 | 09/2026 | **Création de fiches document sans fichier** : le fichier devient optionnel à la création (validation `nullable`, `DocumentService::create` sans version si absent — `current_version_id` null, statut draft) ; champ Fichier optionnel avec mention « À joindre plus tard (onglet Versions) », bouton dynamique « Créer la fiche (sans fichier) » / « Importer et créer » ; badge « sans fichier » sur la fiche et dans la liste Documents + bandeau d'alerte sur la fiche ; rattachement ultérieur de la première version (v1.0) via l'onglet Versions ; approbation toujours bloquée sans fichier (règle 1 V02) | CDG exigeait le fichier dès la création | Saisie des descriptions/métadonnées avant dépôt du fichier |
| 33 | 09/2026 | **Import robuste + Excel** : (a) **encodage** — un CSV non-UTF-8 (Excel « Enregistrer sous CSV » ANSI) est converti automatiquement de Windows-1252 vers UTF-8 (web + CLI) ; (b) **tolérance par ligne** — chaque ligne est traitée dans un try/catch : une erreur (statut, contrainte, encodage…) est consignée « Ligne X » dans le rapport sans interrompre l'import ni générer de 500 ; (c) **Excel .xlsx** — lecteur/générateur natif `XlsxService` (ZipArchive + XML, zéro dépendance : packagist inaccessible) accepté à l'import web et CLI (première feuille, en-tête ligne 1) ; modèle Excel téléchargeable (16 colonnes + 2 exemples) ; le format .xls (ancien binaire) est refusé avec un message clair | Erreur 1366 sur CSV ANSI (accents) et absence de support Excel | Import du registre depuis Excel sans outil CSV, tolérant aux fichiers ANSI |
| 34 | 09/2026 | **Colonnes Référence et Code dans les listes** : Documents — options « Référence » (champ ID de l'import) et « Code » (`document_code`) ajoutées au sélecteur « Colonnes à afficher » (persistance `doc_columns`), en-têtes triables asc/desc (sort=reference et sort=document_code, liste blanche) ; le sous-texte référence sous le titre est retiré (tableau propre) ; Mes documents — colonne Code triable et option « Référence » triable (persistance `my_doc_columns`) | Les champs reference/document_code étaient invisibles dans Documents et non triables | Localisation et tri des documents par code/référence |
| 35 | 09/2026 | **Peaufinage des accès — droits d'administration espaces et IA, création masquée** : nouvelle permission système `admin.spaces` (rôle `tenant_admin` uniquement) — création/renommage/suppression d'espaces et de dossiers refusées (403) aux autres rôles et commandes masquées dans l'écran Espaces (lecture conservée pour tous) ; page de création de document (`documents.create`) interdite (403) aux profils sans la permission (validateur, auditeur) et boutons « Importer » / « Nouveau document » masqués (liste Documents + tableau de bord) ; écran `/admin/ai` (historique IA) réservé à la permission `ai.admin` (403 sinon) ; `roles:sync` crée désormais lui-même les permissions manquantes (`seedPermissions`) — à exécuter après déploiement ; slug mort `admin.ai` retiré des rôles système | Tout membre du tenant pouvait créer/supprimer des espaces et dossiers, accéder à l'historique IA ; les profils sans `documents.create` voyaient des formulaires de création sans pouvoir les soumettre | Phase de peaufinage — interface et accès cohérents avec le RBAC |
| 36 | 09/2026 | **Espaces personnels exclus et protégés** : l'écran « Espaces » n'affiche plus que les espaces partagés (`is_personal = false` — liste, comptage, commandes), comme le fait la liste Documents ; la consultation d'un espace personnel passe exclusivement par « Mes documents personnels » ; renommage (`spaces.rename`) et suppression (`spaces.delete`) d'un espace personnel refusés (403 « Espace personnel géré automatiquement »), même pour l'admin tenant — le cycle de vie reste automatique (création `PersonalSpaceService::ensure`, purge par `tenant:reset`) | Les espaces personnels apparaissaient dans l'écran Espaces (visibilité croisée des noms/volumes entre collègues) et l'admin pouvait les renommer ou supprimer manuellement | Phase de peaufinage — cohérence de l'isolation des espaces personnels |
| 37 | 09/2026 | **Correctifs écran Documents (production)** : (a) le sélecteur « Colonnes métadonnées à afficher… » (renommé « Colonnes à afficher… ») était conditionné à l'existence de `MetadataDefinition` — il disparaissait sur un tenant sans métadonnées personnalisées (ex. après un reset des contenus), masquant aussi les colonnes fixes Référence/Code/Domaine/référentiels ; il s'affiche désormais dès que des colonnes sont proposées (`extraColumns` fixes) ; (b) les regroupements/filtres par référentiels d'application (`ref: poste → pays`, bucket « Non renseigné ») utilisaient `wherePivot('type', …)` dans des closures `whereHas`/`whereDoesntHave` — le SQL généré référençait l'alias « pivot » sans table jointe sous ce nom (erreur MySQL 1054 « Unknown column 'pivot' » : 500 au clic sur une carte vide / « Non renseigné »), masquée en test par SQLite qui tolère la colonne inconnue ; remplacé par la colonne pivot qualifiée explicite `document_referential.type` | 500 « Unknown column 'pivot' » en production sur les regroupements référentiels ; sélecteur de colonnes invisible sur tenant sans métadonnées | Fiabilité de la liste Documents sur MySQL (tests SQLite passaient à tort) |

## Écarts à venir (prévus mais non faits)

| Évolution | Statut |
|-----------|--------|
| OnlyOffice (édition Office en ligne réelle) | En attente de décision (VPS requis) |
| MFA obligatoire + expiration de session (application stricte) | Configurables, application V2 |
| Écran « droits par espace/dossier » (gestion fine par périmètre) | Proposition faite, non retenue en V1 |
| Accusés de lecture obligatoires par poste | Optionnels par document (V02 futur) |
